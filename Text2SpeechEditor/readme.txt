This project is concern a text to speech editor that performs different actions like creating a new empty document, edit a document, save a document, etc. The actions that the editor can perform are include at the .pdf file analytically. The projects designed at eclipse using java language. There are also tests for every action that can performed. 